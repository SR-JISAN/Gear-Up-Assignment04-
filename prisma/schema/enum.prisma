enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum User_Status {
    ACTIVE
    BLOCKED
}

enum product_condition {
    NEW
    USED
    DAMAGED
}

enum product_availability {
    AVAILABLE
    OUT_OF_STOCK
}