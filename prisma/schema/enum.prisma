enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum User_Status {
    ACTIVE
    BLOCKED
}

enum product_condition {
    NEW
    USED
    DAMAGED
}

enum product_availability {
    AVAILABLE
    OUT_OF_STOCK
}

enum order_status {
    PROCESSING
    PLACED 
    CONFIRM 
    PAID
    PICKED_UP 
    RETURNED 
    CANCELLED
}