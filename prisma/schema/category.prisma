model category {
    id Int @id @default(autoincrement()) 
    name String @unique
    products product[]
    created_at  DateTime @default(now())
    updated_at DateTime @updatedAt

   

    @@map("category's")
}